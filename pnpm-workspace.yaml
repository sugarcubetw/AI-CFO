packages:
  - '.'

# Keep the native packages required by the Vinext/Cloudflare build enabled
# when Cloudflare runs `pnpm install --frozen-lockfile`.
onlyBuiltDependencies:
  - esbuild
  - sharp
  - workerd
